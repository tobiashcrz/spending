spring:
  cloud:
    config:
      enabled: false
  flyway:
    enabled: false
    baseline-on-migrate: true
  datasource:
    driver-class-name: org.h2.Driver
    password: sa
    url: "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"